spring:
  application:
    name: m-discovery

server:
  port: 1000

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:1000/eureka/

